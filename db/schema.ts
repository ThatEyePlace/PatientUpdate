import{integer,sqliteTable,text}from"drizzle-orm/sqlite-core";export const patientRecords=sqliteTable("patient_records",{id:integer("id").primaryKey({autoIncrement:true}),firstInitial:text("first_initial").notNull(),lastName:text("last_name").notNull(),previousJobIds:text("previous_job_ids").notNull(),currentJobId:text("current_job_id").notNull().unique(),rx:text("rx").notNull(),createdAt:text("created_at").notNull(),updatedAt:text("updated_at").notNull()});
