import{env}from"cloudflare:workers";import{cookies}from"next/headers";export async function isAuthenticated(){return(await cookies()).get("patientupdate_auth")?.value==="authorized"}export async function init(){await env.DB.prepare("CREATE TABLE IF NOT EXISTS patient_records (id INTEGER PRIMARY KEY AUTOINCREMENT, first_initial TEXT NOT NULL, last_name TEXT NOT NULL, previous_job_ids TEXT NOT NULL, current_job_id TEXT NOT NULL UNIQUE, rx TEXT NOT NULL, created_at TEXT NOT NULL, updated_at TEXT NOT NULL)").run()}export function shape(r:Record<string,unknown>){return{...r,previousJobIds:JSON.parse(String(r.previousJobIds)),rx:JSON.parse(String(r.rx))}}
